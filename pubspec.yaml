name: planner
description: A new Flutter project.

publish_to: 'none' # Remove this line if you are planning to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.2.1  # Updated to the latest version available

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Add assets section with your image path
  assets:
    - lib/screens/assets/images/stopwatch.jpg
